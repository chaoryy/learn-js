//if(age >= 14 && age <= 90)

//if(!(age >= 14 && age <= 90))

if (-1 || 0) alert( 'first' ); //выполнится
if (-1 && 0) alert( 'second' ); //не вывполнится
if (null || -1 && 1) alert( 'third' );  //выполнится